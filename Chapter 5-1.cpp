/* Control Flow - 제어 흐름*/
// Look for the flow chart.
/*
- Halt (중단) : exit(0);
- Jump (점프) : go to, break, continue
- Conditional branches (조건 분기) : if,switch
- Loops(반복 루프) : while,do while,for
- Exceptions (예외처리) : try,catch,throw
*/
#include<iostream>
#include<cstdlib>
using namespace std;

int main()
{
	// Halt

	cout << "I love you" << endl;

	exit(0);// it is simmilar with return 0; but it is more ergent.--> fuck it finish it here.
	// 

	cout << "I love you too" << endl;

	return 0;
}